{
  "name": "gmc-framework",
  "version": "0.0.1",
  "private": false,
  "description": "api for working with several bots as with one",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "smart-wallet-service",
  "devDependencies": {
    "eslint": "^5.5.0",
    "eslint-config-google": "^0.9.1"
  },
  "dependencies": {
    "request": "^2.87.0",
    "telegraf": "^3.22.1"
  },
  "eslintConfig": {
    "extends": "google",
    "parserOptions": {
      "ecmaVersion": 8
    },
    "rules": {
      "max-len": [
        "error",
        {
          "code": 140
        }
      ]
    }
  }
}
